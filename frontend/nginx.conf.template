server {
    listen 80;
    server_name filestore.interndevops.cloud;
    root /usr/share/nginx/html;
    index index.html;

    # React SPA fallback
    location / {
        try_files $uri $uri/ /index.html;
    }
}
